openapi: 3.0.0
info:
  title: Bank Service
  description: APIs that manage financial institution work
  version: 1.0.0

paths:
  /v1/bank/accounts:
    post:
      summary: Save information about a new bank account
      operationId: saveBankAccount
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SaveNewAccountRequest'
      responses: 
        '200':
          description: OK
        '400':
          description: Invalid request
        '500':
          description: Server error
  /v1/bank/institutions:
    post:
      summary: Save Financial Institution Information
      operationId: saveNewBank
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SaveNewBankRequest'
      responses:
        '201':
          description: Financial Institution successfully saved
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SaveNewBankResponse'
        '400':
          description: Invalid request
        '500':
          description: Server error
    get:
      summary: Get all Financial Institutions
      operationId: getFinancialInstitutions
      responses:
        '200':
          description: Currently registered financial institutions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FinancialInstitutionsResponse'
        '400':
          description: Invalid request
        '500':
          description: Server Error
  /v1/bank/users/{userID}/banks:
    get:
      summary: Get a user's financial institution information
      operationId: getUserFinancialProfile
      parameters:
        - name: userID
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Successfully fetched user's bank details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserFinancialProfile'
        '400':
          description: Invalid request
        '500':
          description: Server error
  /v1/bank/plaid/link-token:
    post:
      summary: Create a Plaid Link token so a frontend can launch Plaid Link
      operationId: createLinkToken
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateLinkTokenRequest'
      responses:
        '201':
          description: Link token created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CreateLinkTokenResponse'
        '400':
          description: Invalid request
        '500':
          description: Server error
  /v1/bank/plaid/exchange:
    post:
      summary: Exchange a Plaid Link public_token for an access_token and save the linked accounts
      operationId: exchangePublicToken
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ExchangePublicTokenRequest'
      responses:
        '201':
          description: Accounts linked successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExchangePublicTokenResponse'
        '400':
          description: Invalid request
        '500':
          description: Server error

components:
  schemas:
    SaveNewBankRequest:
      type: object
      required:
        - bank_name
      properties:
        bank_name:
          type: string
    SaveNewBankResponse:
      type: object
      required:
        - bank_id
      properties:
        bank_id:
          type: integer

    SaveNewAccountRequest:
      type: object
      required:
        - user_id
        - bank_name
        - accounts
        - access_token
      properties:
        user_id:
          type: integer
        bank_name:
          type: string
        accounts:
          $ref: '#/components/schemas/Account'
        access_token:
          type: string

    FinancialInstitutionsResponse:
      type: object
      required:
        - banks
      properties:
        banks:
          type: array
          items:
            $ref: '#/components/schemas/BankItem'
    BankItem:
      type: object
      required:
        - bank_id
        - bank_name
      properties:
        bank_id:
          type: integer
        bank_name:
          type: string

    UserFinancialProfile:
      type: object
      required:
        - banks
      properties:
        user_id:
          type: integer
        banks:
          type: array
          items:
            $ref: '#/components/schemas/UserAccountInfo'
    
    UserAccountInfo:
      type: object
      required:
        - bank_name
        - accounts
      properties:
        bank_name:
          type: string
        accounts:
          type: array
          items:
            $ref: '#/components/schemas/Account'

    Account:
      type: object
      required:
        - account_id
        - account_type
      properties:
        account_id:
          type: string
        account_type:
          type: string

    CreateLinkTokenRequest:
      type: object
      required:
        - user_id
      properties:
        user_id:
          type: integer
    CreateLinkTokenResponse:
      type: object
      required:
        - link_token
        - expiration
      properties:
        link_token:
          type: string
        expiration:
          type: string

    ExchangePublicTokenRequest:
      type: object
      required:
        - user_id
        - bank_name
        - public_token
      properties:
        user_id:
          type: integer
        bank_name:
          type: string
        public_token:
          type: string
    ExchangePublicTokenResponse:
      type: object
      required:
        - bank_id
        - accounts
      properties:
        bank_id:
          type: integer
        accounts:
          type: array
          items:
            $ref: '#/components/schemas/Account'
